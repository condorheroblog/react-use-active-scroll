{
	"name": "test-dts-bundle-repro",
	"type": "module",
	"version": "0.0.0",
	"private": true,
	"description": "Minimal reproduction for unplugin-dts bundleTypes + outDirs behavior.",
	"scripts": {
		"build": "rm -rf dist && vite build",
		"build:inline": "rm -rf dist && vite build --config vite.config.inline.ts",
		"typecheck": "tsc --noEmit"
	},
	"devDependencies": {
		"@microsoft/api-extractor": "^7.59.0",
		"typescript": "^6.0.3",
		"unplugin-dts": "^1.1.0",
		"vite": "8.2.2"
	}
}
